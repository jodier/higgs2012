#ifndef ISEMPLUSPLUSHELPER_H
#define ISEMPLUSPLUSHELPER_H
#include <iostream>
#include <fstream>
#include <string>
using std::ptrdiff_t;
#include "egammaEvent/egamma.h"
#include "egammaAnalysisUtils/IsEMPlusPlusDefs.h"

namespace isEMPlusPlusHelper{

  bool IsLoosePlusPlus(const egamma* eg, egammaMenu::egMenu menu = egammaMenu::eg2012, 
		       bool debug=false,
		       bool isTrigger=false);
  bool IsLoosePlusPlus0(const egamma* eg,egammaMenu::egMenu menu =egammaMenu::eg2012,
		       bool debug=false,
		       bool isTrigger=false);
  bool IsLoosePlusPlus1(const egamma* eg,egammaMenu::egMenu menu=egammaMenu::eg2012,
		       bool debug=false,
		       bool isTrigger=false);
  bool IsMediumPlusPlus(const egamma* eg,egammaMenu::egMenu menu=egammaMenu::eg2012,
			bool debug=false,
			bool isTrigger=false);

  bool IsTightPlusPlus(const egamma* eg, egammaMenu::egMenu menu=egammaMenu::eg2012, 
			bool debug=false,
			bool isTrigger=false);
 
}

#endif //
